# Force a flat node_modules layout so Hugo's TailwindCSS transform can
# locate `tailwindcss` and treat it as an executable Node script —
# avoids the "binary 'tailwindcss' is not a Node.js script" error that
# pnpm's symlink-style install produces on Netlify.
node-linker=hoisted
shamefully-hoist=true
