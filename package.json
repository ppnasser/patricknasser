{
  "name": "patricknasser",
  "private": true,
  "version": "1.0.0",
  "description": "patricknasser.com.br — built with Hugo Blox",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify",
    "pagefind": "pagefind --site public"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "pagefind": "^1.1.1"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^4.3.0"
  }
}
